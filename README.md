# gtk-webkit-c-go-bridge
A minimal C-Go bridge for GTK WebKit.Only ~382 lines of C. C does the absolute minimum (multi-window spawning + JSON courier) and hands everything else to Go. Clean and intentionally low surface area.Works today on Linux (GTK WebKit + X11). iOS/Android support planned.
